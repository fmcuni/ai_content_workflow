NEXT_PUBLIC_API_BASE=http://localhost:8000
NEXT_PUBLIC_DEFAULT_EDITOR_EMAIL=editor@bowtie.local
# Local dev runs against the Python backend, which has no auth routes. Set this
# to bypass the login gate (proxy.ts) and SSE tickets. Leave UNSET in prod.
AUTH_DISABLED=true
