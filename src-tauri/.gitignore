# Rust build output
/target

# Tauri-generated schemas/bindings
/gen

# Build-time artifacts assembled by scripts/desktop/* — never committed:
#  - binaries/    PyInstaller backend binary (target-triple suffixed)
#  - resources/   Next standalone tree + bundled node
/binaries
/resources
