{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "external-links",
  "description": "Allow the frontend (served from the local Next server) to open external URLs in the system browser via the opener plugin.",
  "windows": ["main"],
  "remote": {
    "urls": ["http://localhost:3000/*", "http://127.0.0.1:3000/*"]
  },
  "permissions": ["opener:allow-open-url"]
}
